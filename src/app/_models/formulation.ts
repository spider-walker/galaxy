import {FormulationRM} from "./formulation-rm";

export class Formulation {
    id: number;
    production_code: string;
    description: string;
    color: string;
    status: string;
    creation_date: string;
    formulationRM:FormulationRM;
}
